import { ChangeDetectionStrategy, Component } from '@angular/core';

@Component({
  selector: 'lg-section-markers-image',
  templateUrl: './section-markers-image.html',
  changeDetection: ChangeDetectionStrategy.OnPush,
})
export class SectionMarkersImage {}
